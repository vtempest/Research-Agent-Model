/**
 * Remote cursor overlay for the Plate/Yjs route.
 *
 * The implementation is the Plate registry's own `remote-cursor-overlay`
 * component (generated into `./ui/`); this module is the stable path the editor
 * imports, so a registry re-run cannot change the editor's import surface.
 */

export { RemoteCursorOverlay } from './ui/remote-cursor-overlay';
